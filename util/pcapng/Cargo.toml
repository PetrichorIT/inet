[package]
name = "pcapng"
version = "0.1.0"
edition = "2024"

[features]
default = []
test-util = ["dep:des"]

[dependencies]
bitflags = ">2.6.0"
des = { version = "*", optional = true }
bytes-io = { path = "../bytes-io" }

[dev-dependencies]
types = { path = "../types" }
