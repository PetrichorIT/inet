[package]
name = "pcapng"
version = "0.1.0"
edition = "2021"

[features]
default = ["test-util"]
test-util = ["dep:des"]

[dependencies]
bytepack = { path = "../bytepack" }
bitflags = "*"
des = { version = "*", optional = true }

[dev-dependencies]
types = { path = "../types" }
