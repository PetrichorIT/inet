[package]
name = "types"
version = "0.1.0"
edition = "2021"

[features]
default = []
uds = []

[dependencies]
bitflags = ">2.6.0"
bytepack = { path = "../bytepack" }
des = { version = "*", features = ["full"] }
