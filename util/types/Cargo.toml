[package]
name = "types"
version = "0.1.0"
edition = "2021"

[features]
default = []
uds = []

[dependencies]
bitflags = ">2.6.0"
bytepack = { path = "../bytepack" }
bytes-io = { path = "../bytes-io" }
des = { version = "*", features = ["full"] }
